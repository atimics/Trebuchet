name: CI

on:
  pull_request:
  push:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  test:
    name: Test
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Set up Node
        uses: actions/setup-node@v6
        with:
          node-version: 22.12.0
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Syntax check
        run: npm run check:syntax

      - name: Unit tests
        run: npm test

      - name: Critical audit gate
        run: npm audit --audit-level=critical

  build:
    name: Build ${{ matrix.name }}
    needs: test
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: macOS arm64
            os: macos-15
            smoke_command: npm run build:mac -- --arm64 --dir --publish=never
            release_command: npm run build:mac -- --arm64 --publish=never
            artifact: trebuchet-macos-arm64
            paths: |
              dist/*.dmg
              dist/*.zip
          - name: macOS x64
            os: macos-15-intel
            smoke_command: npm run build:mac -- --x64 --dir --publish=never
            release_command: npm run build:mac -- --x64 --publish=never
            artifact: trebuchet-macos-x64
            paths: |
              dist/*.dmg
              dist/*.zip
          - name: Windows
            os: windows-latest
            smoke_command: npm run build:win -- --dir --publish=never
            release_command: npm run build:win -- --publish=never
            artifact: trebuchet-windows
            paths: |
              dist/*.exe
              dist/*.yml
          - name: Linux
            os: ubuntu-latest
            smoke_command: npm run build:linux -- --dir --publish=never
            release_command: npm run build:linux -- --publish=never
            artifact: trebuchet-linux
            paths: |
              dist/*.AppImage
              dist/*.deb

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Set up Node
        uses: actions/setup-node@v6
        with:
          node-version: 22.12.0
          cache: npm

      - name: Install Linux packaging dependencies
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y libarchive-tools rpm

      - name: Install dependencies
        run: npm ci

      - name: Build package smoke
        if: github.event_name == 'pull_request' || (github.event_name == 'push' && github.ref != 'refs/heads/main')
        run: ${{ matrix.smoke_command }}

      - name: Build release package
        if: github.event_name == 'workflow_dispatch' || github.ref == 'refs/heads/main'
        run: ${{ matrix.release_command }}

      - name: Upload build artifact
        if: github.event_name == 'workflow_dispatch' || github.ref == 'refs/heads/main'
        uses: actions/upload-artifact@v7
        with:
          name: ${{ matrix.artifact }}
          path: ${{ matrix.paths }}
          if-no-files-found: error
